console.log(parseInt("3.1415"));
// Returns 3 as the function stops when it finds anything that is not numerical e.g. the dot
// To convert this to a real float you need to use parseFloat()
console.log(parseFloat("3.1415"));
