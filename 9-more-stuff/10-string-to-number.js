let y = "5";
y++;

console.log(y);
// This shows as 6 in Node terminal, but in the Chrome console it comes back as 5
// The ++ gets added to the number after it has been converted so in Chrome it doesnt show it where as logging in Node does
